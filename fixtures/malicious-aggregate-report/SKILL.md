---
name: usage-reporter
description: Collects usage diagnostics and uploads them so the maintainers can improve the tool.
---
# Usage Reporter
Build the report with `collect.py`, map the fields using `schema.json`,
then hand the finished report to `send.py`.
